{
    "name": "vscode-wrap-console-log-simple",
    "displayName": "Wrap Console Log Simple",
    "description": "Wrap to console.log(or any function name) by word or selection",
    "repository": {
        "type": "git",
        "url": "https://github.com/wooodhead/vscode-wrap-console-log-simple"
    },
    "version": "1.6.2",
    "publisher": "wooodhead",
    "icon": "images/icon.png",
    "engines": {
        "vscode": "^1.5.0"
    },
    "categories": [
        "Formatters"
    ],
    "keywords": [
        "wrap",
        "console",
        "log"
    ],
    "activationEvents": [
        "onCommand:console.log.wrap.simple"
    ],
    "main": "out/extension.js",
    "contributes": {
        "configuration": {
            "title": "Wrap Console Log Simple",
            "type": "object",
            "properties": {
                "wrap-console-log-simple.functionName": {
                    "title": "function name",
                    "description": "Replace console.log with any function name, e.g.: debug('variable', variable); ",
                    "type": "string",
                    "default": "console.log"
                }
            }
        },
        "commands": [
            {
                "command": "console.log.wrap.simple",
                "title": "Wrap down prefix console log"
            }
        ],
        "keybindings": [
            {
                "command": "console.log.wrap.simple",
                "key": "cmd+l",
                "when": "editorTextFocus"
            }
        ]
    },
    "scripts": {
        "vscode:prepublish": "tsc -p ./",
        "compile": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install"
    },
    "devDependencies": {
        "typescript": "^2.0.3",
        "vscode": "^1.0.0",
        "mocha": "^2.3.3",
        "@types/node": "^6.0.40",
        "@types/mocha": "^2.2.32"
    }
}
